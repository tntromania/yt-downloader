[phases.setup]
nixPkgs = ["nodejs_18", "ffmpeg", "python3", "curl"]

[phases.install]
cmds = [
    "npm install",
    "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /tmp/yt-dlp",
    "chmod a+rx /tmp/yt-dlp"
]

[start]
cmd = "node server.js"